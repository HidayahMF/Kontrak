import{useEffect,useState}from'react';import{Link,useNavigate,useParams}from'react-router-dom';import{ArrowLeft,Trash2,Edit}from'lucide-react';import{api}from'../api';import{Contract,AuthUser}from'../types';import{ErrorMessage,Spinner}from'../components/States';const d=(x:string)=>new Date(`${x.slice(0,10)}T00:00:00`).toLocaleDateString('id-ID');export function ContractDetail(){const{id}=useParams(),nav=useNavigate();const[item,setItem]=useState<Contract>(),[user,setUser]=useState<AuthUser>(),[error,setError]=useState('');useEffect(()=>{api.contract(Number(id)).then(setItem).catch(x=>setError(x.message));api.me().then(x=>setUser(x.user))},[id]);if(error)return <ErrorMessage message={error}/>;if(!item)return <Spinner/>;const remove=async()=>{if(!confirm(`Hapus data kontrak ${item.employeeName}? Data yang dihapus tidak dapat dikembalikan.`))return;try{await api.remove(item.id);nav('/dashboard')}catch(x){setError(x instanceof Error?x.message:'Kontrak gagal dihapus.')}};return <div className="mx-auto max-w-2xl"><Link to="/dashboard" className="mb-5 inline-flex items-center gap-2 text-sm text-bmc-muted"><ArrowLeft size={16}/>Kembali ke Dashboard</Link><div className="border border-bmc-border bg-white p-6 sm:p-8"><p className="mb-2 text-xs font-semibold uppercase tracking-widest text-bmc-navy">Monitoring kontrak</p><h1 className="text-xl font-semibold">Detail Kontrak Karyawan</h1><dl className="mt-7 grid gap-5 sm:grid-cols-2"><div><dt className="label">Nama</dt><dd>{item.employeeName}</dd></div><div><dt className="label">NIP</dt><dd>{item.nip}</dd></div><div><dt className="label">Tanggal Awal Kontrak</dt><dd>{d(item.startDate)}</dd></div><div><dt className="label">Tanggal Akhir Kontrak</dt><dd>{d(item.endDate)}</dd></div><div><dt className="label">Status</dt><dd>{item.status}</dd></div><div><dt className="label">Sisa hari</dt><dd>{item.remainingDays<0?`Berakhir ${Math.abs(item.remainingDays)} hari lalu`:`${item.remainingDays} hari`}</dd></div><div><dt className="label">Dibuat oleh</dt><dd>{item.createdByName} ({item.createdByNip})</dd></div><div><dt className="label">Tanggal dibuat</dt><dd>{new Date(item.createdAt).toLocaleDateString('id-ID')}</dd></div><div><dt className="label">Terakhir diperbarui</dt><dd>{new Date(item.updatedAt).toLocaleDateString('id-ID')}</dd></div></dl><div className="mt-8 flex flex-wrap gap-3"><Link to={`/contracts/${item.id}/edit`} className="btn-primary"><Edit size={16}/>Edit Kontrak</Link>{user?.role==='ADMIN'&&<button onClick={remove} className="btn-secondary text-bmc-error"><Trash2 size={16}/>Hapus Kontrak</button>}</div></div></div>}
